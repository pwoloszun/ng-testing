import { visitAdvQueryingPage } from './helpers';

describe('infinite list', () => {
  xit('should load 30 feeds on inti', () => {
  });

  xit('should load 10 more feeds on every scroll down', () => {
  });

  xit('should show spiner while loading and hide afterwards', () => {
  });
});
